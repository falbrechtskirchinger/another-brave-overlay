# SPDX-FileCopyrightText: Copyright 2025 Florian Albrechtskirchinger <falbrechtskirchinger@gmail.com>
#
# SPDX-License-Identifier: MIT

name: 🛡️ Merge ebuild (nightly)

on:
  workflow_run:
    workflows: ['🧪 Merge ebuilds']
    types: [completed]

jobs:
  check-status:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Collect test results
        id: test-results
        run: python scripts/test_ebuilds.py -v --collect-test-results --from-event --read-variables
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.INTEGRATION_PAT }}

      - name: Report status
        run: |
          if [[ -z "${{ fromJSON(steps.test-results.outputs.test_results).nightly.conclusion }}" ]]; then
            echo "⚠️ No 'nightly' test result available."
            exit 1
          elif [[ "${{ fromJSON(steps.test-results.outputs.test_results).nightly.cached }}" == "true" ]]; then
            echo "💡 Test result is cached."
          fi

          if [[ "${{ fromJSON(steps.test-results.outputs.test_results).nightly.conclusion }}" == "success" ]]; then
            echo "✅ 'Nightly' ebuild merge succeeded."
            exit 0
          else
            echo "❌ 'Nightly' ebuild merge failed."
            exit 1
          fi
